type: object
properties:
  streams:
    type: array
    items:
      - type: object
        properties:
          name: pdf
          namespace:
            description: "namespace the data is associated with"
            type: string
          dir_uris:
            type: array
            items:
              type: string
          sync_mode:
            type: string
            enum:
              - full_refresh
              - incremental
      - type: object
        properties:
          name: txt
          namespace:
            description: "namespace the data is associated with"
            type: string
          dir_uris:
            type: array
            items:
              type: string
          sync_mode:
            type: string
            enum:
              - full_refresh
              - incremental
